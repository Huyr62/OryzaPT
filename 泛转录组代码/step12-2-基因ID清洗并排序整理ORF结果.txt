awk '{ 
    # 去掉第一列尾部的数字部分
    gene_id = $1; 
    sub(/\.[0-9]+$/, "", gene_id); 
    
    # 输出新的列（去掉尾部数字后的基因ID），原始的第一列和第三列
    print gene_id, $1, $3 
}' found_gene_orfs.txt | sort -k1,1 > sorted_processed_gene_orfs.txt
